apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer-register.fullname" . }}
  labels:
    {{- include "customer-register.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "customer-register.selectorLabels" . | nindent 4 }}
